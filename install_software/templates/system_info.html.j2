<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System Information</title>
</head>
<body>
    <h1>System Information</h1>
    <p><strong>Hostname:</strong> {{ ansible_facts['hostname'] }}</p>
    <p><strong>CPU:</strong> {{ ansible_facts['processor_cores'] }} cores</p>
    <p><strong>RAM:</strong> {{ ansible_facts['memtotal_mb'] }} MB</p>
    <p><strong>Disks:</strong> {{ ansible_facts['mounts'] | map(attribute='device') | join(', ') }}</p>
</body>
</html>

